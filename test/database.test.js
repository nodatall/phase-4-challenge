const expect = require('../config/testSetup')
const { getAlbums, getAlbumsByID } = require('../database.js')

describe('database', () => {
  it('should do things', () => {
    expect(2).to.equal(2)

  })
})
